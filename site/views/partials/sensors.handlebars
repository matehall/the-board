<div class="sensorWidget">	
	{{#each partials.sensors.readings}}
    <div class="sensors">
    	<p class="name">{{name}}</p>
    	<p class="value">{{value}}</p>
    </div>
    {{/each}}
    <small> Source: <a href= "http://www.mysensors.com" >Powerd by MySensors</a></small>
</div>
